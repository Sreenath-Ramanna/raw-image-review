name: raw_viewer
description: Camera RAW image viewer for Linux desktop.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  ffigen: ^12.0.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
